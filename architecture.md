# 소프트웨어 아키텍처: 한양대학교 수강신청 자동화

이 문서는 대상 웹사이트(한양대학교 수강신청 포털)의 기술적 구조와 이를 자동화하기 위해 본 프로젝트가 어떻게 설계되었는지 설명합니다.

## 1. 대상 웹사이트 구조 (Target Website Architecture)

한양대학교 수강신청 시스템은 여러 보안 및 트래픽 제어 메커니즘이 적용된 복잡한 웹 애플리케이션입니다.

### 1.1 인증 시스템 (SSO)

- **프로토콜**: `JSESSIONID` 쿠키를 사용하는 세션 기반 인증입니다.
- **보안 절차**:
  - **RSA 암호화**: 아이디와 비밀번호는 **평문으로 저장·전송하지 않습니다**. 클라이언트는 서버 공개키를 받아 PKCS#1 v1.5 방식으로 자격 증명을 암호화해 로그인합니다.
  - **동적 토큰 (`tk`)**: 로그인 후 `sulg.do` HTML 내의 토큰(`tk`)이 후속 API 호출에 필요합니다.

### 1.2 트래픽 제어 (NetFunnel)

- **매커니즘**: 서버 과부하 제어를 위해 STCLab NetFunnel을 사용합니다.
- **워크플로우**:
  1. **티켓 요청 (Opcode 5001)**: 대기열 티켓 키를 발급받습니다.
  2. **활성화 (Opcode 5002)**: 발급 키를 사용해 입장 여부를 확인합니다.
  3. **검증**: 수강신청 API는 유효한 `IN_NETFUNNEL_KEY`가 포함되어야 합니다.

### 1.3 주요 API 엔드포인트

| 엔드포인트                           | 메서드 | 목적            | 주요 파라미터                    |
| :----------------------------------- | :----- | :-------------- | :------------------------------- |
| `/login/publicTk.do`                 | POST   | RSA 공개키 획득 | `keyNm="sso_002"`                |
| `/login/lgnps.do`                    | POST   | 로그인 수행     | 암호화된 `userId`, `password`    |
| `/sugang/sulg.do`                    | GET    | 메인 페이지     | HTML에서 `tk` 토큰 추출          |
| `/SgscAct/findHeemangSuupSearchs.do` | POST   | 강의 검색       | `suupNo`(수업번호) 반환          |
| `/ts.wseq` (NetFunnel)               | GET    | 티켓 발급/확인  | `opcode=5001`, `opcode=5002`     |
| `/SgscAct/hyuHaksaengSgsc.do`        | POST   | 수강신청 요청   | `IN_SUUP_NO`, `IN_NETFUNNEL_KEY` |

---

## 2. 프로젝트 아키텍처 (The Solution)

이 자동화 도구는 대화형 실행 흐름을 중심으로 설계되며, 자격 증명 저장소와 신청 모드를 분리해 안전성과 재시작 복구성을 높입니다.

### 2.1 파일 구조

- **`main.py`**: 인증, 모드 선택, 과목 조회, 신청 오케스트레이션.
- **`credentials.py`**: Fernet 기반 자격 증명 저장/마이그레이션/검증.
- **`workflow.py`**: 예약 카운트다운 및 모드별 라운드 로직.
- **`config.json`**: 하위 호환용 템플릿(현재 런타임 동작은 직접 참조하지 않음).
- **`secrets.json`**: 과거 버전과의 마이그레이션 입력.
- **`secrets.json.example`**: 마이그레이션 가이드 입력 예시.

### 2.2 핵심 로직 (`main.py`)

애플리케이션은 다음 단계를 따릅니다.

#### A. 인증 및 세션

- 첫 실행이거나 유효한 암호화 자격 증명이 없을 때 `questionary`로 ID/비밀번호를 입력받고, 실제 로그인 테스트를 통해 실시간 검증한 뒤 저장합니다.
- 이후 실행에서는 암호화된 로컬 저장소를 우선 사용해 재로그인 오버헤드를 줄입니다.
- `secrets.json`이 존재하면 우선 검증 후 자동 마이그레이션 후 사용.

#### B. RSA 모듈 (`rsa_encrypt`)

- 포털의 `enc_core.js` 동작을 따라 PKCS#1 v1.5 패딩 기반 암호화를 수행합니다.

#### C. NetFunnel 로직 (`get_netfunnel_key`)

- 단계:
  1. `opcode=5001`로 티켓 발급
  2. 발급 키로 `opcode=5002` 체크인
- 각 신청 시도마다 티켓 유효성을 보장하도록 재획득.

#### D. 실행 파이프라인 (Execution Pipeline)

1. 자격 증명 로드/검증
2. 로그인 세션 생성
3. `tk` 토큰 추출
4. 희망수업 조회
5. 사용자 선택/우선순위 지정
6. 모드 선택
   - `예약 수강신청`: 시각 입력 및 카운트다운 (`YYYY-MM-DD HH:MM:SS`, `HH:MM:SS` 표시)
   - `바로 취케팅`: 즉시 시작
7. 신청 실행
   - 예약: 과목별 1회 시도 후 실패 과목만 전달
   - 바로 취케팅: 실패 과목만 유지해 성공할 때까지 무제한 라운드 로빈
8. 요약 출력
   - `Ctrl+C` 입력 시 완료/대기 과목 요약을 표시하고 종료

## 3. 요약

1. RSA 암호화 동작으로 로그인 자격 증명을 처리합니다.
2. NetFunnel의 2단계 대기열을 시퀀싱해 접근권을 획득합니다.
3. 모드 기반 실행 정책으로 예약 시점 정확성 + 실시간 즉시 재시도를 지원합니다.
4. Fernet 암호화와 플랫폼 경로 저장소로 자격 증명 생애주기를 통제합니다.
