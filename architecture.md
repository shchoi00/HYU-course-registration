# 소프트웨어 아키텍처: 한양대학교 수강신청 자동화

이 문서는 대상 웹사이트(한양대학교 수강신청 포털)의 기술적 구조와 이를 자동화하기 위해 본 프로젝트가 어떻게 설계되었는지 설명합니다.

## 1. 대상 웹사이트 구조 (Target Website Architecture)

한양대학교 수강신청 시스템은 여러 보안 및 트래픽 제어 메커니즘이 적용된 복잡한 웹 애플리케이션입니다.

### 1.1 인증 시스템 (SSO)

- **프로토콜**: `JSESSIONID` 쿠키를 사용하는 세션 기반 인증입니다.
- **보안 절차**:
  - **RSA 암호화**: 아이디와 비밀번호는 **절대 평문으로 전송되지 않습니다**. 클라이언트는 서버에서 동적 RSA 공개키(`publicTk.do`)를 받아, 자격 증명을 암호화(PKCS#1 v1.5)하여 전송해야 합니다.
  - **동적 토큰 (`tk`)**: 로그인 후 메인 페이지(`sulg.do`) HTML 내에 고유 토큰(`tk`)이 숨겨져 있습니다. 이후 모든 주요 API 호출(강의 검색, 신청) 시 이 토큰을 URL 파라미터로 포함해야 합니다.

### 1.2 트래픽 제어 (NetFunnel)

- **매커니즘**: 서버 과부하를 방지하기 위해 타사 솔루션(STCLab NetFunnel)을 사용합니다.
- **워크플로우**:
  1.  **티켓 요청 (Opcode 5001)**: 클라이언트가 대기열 티켓을 요청합니다.
  2.  **활성화 확인 (Opcode 5002)**: 발급받은 티켓이 활성화되어 서버 진입이 가능한지 확인합니다.
  3.  **검증**: 수강신청 API(`hyuHaksaengSgsc.do`)는 요청과 함께 전송된 `IN_NETFUNNEL_KEY`가 유효한지 검증합니다.

### 1.3 주요 API 엔드포인트

| 엔드포인트                           | 메서드 | 목적            | 주요 파라미터                    |
| :----------------------------------- | :----- | :-------------- | :------------------------------- |
| `/login/publicTk.do`                 | POST   | RSA 공개키 획득 | `keyNm="sso_002"`                |
| `/login/lgnps.do`                    | POST   | 로그인 수행     | 암호화된 `userId`, `password`    |
| `/sugang/sulg.do`                    | GET    | 메인 페이지     | HTML에서 `tk` 토큰 추출          |
| `/SgscAct/findHeemangSuupSearchs.do` | POST   | 강의 검색       | `suupNo`(수업번호) 반환          |
| `/ts.wseq` (NetFunnel)               | GET    | 티켓 발급/확인  | `opcode=5001`, `opcode=5002`     |
| `/SgscAct/hyuHaksaengSgsc.do`        | POST   | 수강신청 요청   | `IN_SUUP_NO`, `IN_NETFUNNEL_KEY` |

---

## 2. 프로젝트 아키텍처 (The Solution)

이 자동화 도구는 합법적인 브라우저 세션을 모방하면서 사용자 상호작용을 자동화하도록 설계되었습니다.

### 2.1 파일 구조

보안과 유지보수성을 위해 설정, 비밀 정보, 로직을 분리했습니다.

- **`main.py`**: 핵심 애플리케이션 로직입니다.
- **`config.json`**: 운영 설정 (대상 과목, 스케줄링, 재시도 정책).
- **`secrets.json`**: 민감한 자격 증명 (ID/비밀번호). **(.gitignore 처리됨)**

### 2.2 핵심 로직 (`main.py`)

애플리케이션은 실행 내내 쿠키와 헤더를 유지하는 단일 `requests.Session` 객체를 중심으로 동작합니다.

#### A. 세션 관리 (Session Management)

- **헤더**: 기본적인 봇 탐지를 우회하기 위해 표준 브라우저 헤더(`User-Agent`, `Referer`, `Origin`, `sec-ch-ua`)를 모방합니다.
- **쿠키**: `JSESSIONID` 및 기타 세션 트래커를 자동으로 처리합니다.

#### B. 암호화 모듈 (`rsa_encrypt`)

- 포털의 `enc_core.js`에 있는 자바스크립트 RSA 로직을 재구현했습니다.
- Python의 `math`와 `base64` 라이브러리를 사용하여 브라우저와 동일한 방식(PKCS#1 v1.5 패딩)으로 RSA 암호화를 수행합니다.

#### C. NetFunnel 로직 (`get_netfunnel_key`)

- NetFunnel 서버가 요구하는 특정 2단계 핸드셰이크를 구현했습니다:
  1.  **티켓 요청**: Opcode 5001을 호출하여 새 키를 받습니다.
  2.  **키 활성화**: 새 키로 Opcode 5002를 호출하여 활성 대기열에 "체크인"합니다.
- 이를 통해 수강신청 서버로 전송되는 키가 유효하고 "활성" 상태임을 보장합니다.

#### D. 실행 파이프라인 (Execution Pipeline)

스크립트는 선형적이고 견고한 파이프라인을 따릅니다:

1.  **대기 (Wait)**: 현재 시간이 예약된 시작 시간인지 확인합니다.
2.  **로그인 (Login)**: RSA 암호화된 SSO 로그인을 수행합니다.
3.  **스크래핑 (Scrape)**: 메인 페이지에 접근하여 동적 `tk` 토큰을 스크래핑합니다.
4.  **검색 (Search)**: 사용자의 "희망 수업" 목록을 조회하여 내부 과목 ID(`suupNo`)를 얻습니다.
5.  **루프 (Loop - 각 과목별)**:
    - **대기열 (Queue)**: 새로운 NetFunnel 키를 획득합니다.
    - **신청 (Register)**: 수강신청 요청을 전송합니다.
    - **재시도 (Retry)**: 요청 실패 시(예: 서버 타임아웃) 반복합니다.

## 3. 요약

이 프로젝트는 다음과 같은 방법으로 일반 HTTP 클라이언트와 복잡한 대학 포털 사이의 간극을 해결했습니다:

1.  클라이언트 측 암호화 로직을 **역공학(Reverse-engineering)**하여 구현.
2.  독점적인 트래픽 제어 프로토콜(NetFunnel)을 **시뮬레이션**.
3.  브라우저와 일관된 세션 상태를 **유지**.
