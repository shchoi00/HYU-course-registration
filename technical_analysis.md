# 한양대학교 수강신청 시스템 기술 분석 및 동작 원리

이 문서는 한양대학교 수강신청 시스템의 구조, 인증 로직, 수강신청 로직, 그리고 이를 자동화하기 위한 기술적 접근 방법을 설명합니다.

## 1. 전체적인 웹사이트 구조 (Architecture)

한양대학교 수강신청 시스템은 전형적인 **세션 기반(Session-based)** 웹 애플리케이션으로, 다음과 같은 특징을 가집니다.

1.  **세션 관리**: `JSESSIONID` 쿠키를 사용하여 사용자를 식별합니다.
2.  **보안 (Security)**:
    - **RSA 암호화**: 로그인 시 비밀번호를 평문으로 전송하지 않고, 서버에서 받은 공개키로 RSA 암호화하여 전송합니다.
    - **토큰 (Token)**: `tk`라는 동적 토큰을 URL 파라미터로 계속 달고 다녀야 합니다. 이 토큰은 로그인 직후 메인 페이지(`sulg.do`) HTML 내에 포함되어 있습니다.
    - **헤더 검증**: `Referer`, `Origin`, `sec-ch-ua` 등의 헤더를 검사하여 브라우저 외의 접근을 차단하려 합니다.
3.  **트래픽 제어 (Traffic Control)**: **NetFunnel**이라는 솔루션을 사용하여 대기열을 관리합니다. 수강신청 요청 전 반드시 NetFunnel 서버로부터 '티켓(Key)'을 발급받아야 합니다.

---

## 2. 인증 로직 (Authentication Logic) - SSO

로그인 과정은 단순한 ID/PW 전송이 아니라, **공개키 암호화 방식**을 사용합니다.

### 단계 1: 공개키 획득 (`publicTk.do`)

- **요청**: `POST /publicTk.do`
- **내용**: 사용자 ID(`encStr`)와 키 이름(`keyNm="sso_002"`)을 보냅니다.
- **응답**: 서버가 512비트 RSA 공개키(Modulus)를 줍니다.

### 단계 2: RSA 암호화 (Client-Side Encryption)

- 사용자의 ID와 비밀번호를 서버에서 받은 공개키로 암호화합니다.
- **알고리즘**: RSA (PKCS#1 v1.5 padding type 2)
- **특징**: `enc_core.js`라는 자바스크립트 라이브러리에서 수행되며, 평문을 Base64로 인코딩한 뒤 50자 단위로 쪼개서 암호화하고 다시 합치는 방식을 사용합니다.

### 단계 3: 로그인 요청 (`lgnps.do`)

- **요청**: `POST /lgnps.do`
- **내용**: 암호화된 `userId`, `password`를 전송합니다.
- **결과**: 성공 시 세션 쿠키(`JSESSIONID`)가 발급되며, 이후 모든 요청에 이 쿠키를 포함해야 인증된 상태로 유지됩니다.

---

## 3. 수강신청 로직 (Registration Logic)

수강신청은 **토큰 확보 -> 검색 -> 대기열 통과 -> 신청** 순서로 진행됩니다.

### 단계 1: 토큰 및 정보 추출 (`sulg.do`)

- 로그인 후 메인 페이지 HTML을 파싱하면 다음과 같은 정보가 숨어있습니다.
  - `tk`: 매 요청마다 필요한 인증 토큰 (예: `tk=96c7fb6e...`)
  - `pgmId`, `menuId`: 메뉴 식별자
  - `gaeinNo`: 개인 고유 번호

### 단계 2: 희망수업 조회 (`findHeemangSuupSearchs.do`)

- 미리 담아둔 희망수업 목록을 조회하여 신청할 과목의 내부 번호(`suupNo`)와 속성(`jojikGbCd`, `sgscGb` 등)을 파악합니다. 신청 요청 시 이 값들이 정확히 일치해야 합니다.

### 단계 3: 대기열 진입 (NetFunnel)

- 수강신청 버튼을 누르면 브라우저는 먼저 NetFunnel 서버(`hynfad-3391.netfunnel.stclab.com`)에 접근합니다.
- **Opcode 5001**: "나 줄 설게, 티켓 줘" (Ticket 발급)
- **Opcode 5002**: "내 티켓 유효해? 들어가도 돼?" (Check-in / Key 활성화)
- 이 과정을 통과하면 유효한 `key`를 얻게 됩니다.

### 단계 4: 수강신청 요청 (`hyuHaksaengSgsc.do`)

- 최종적으로 학교 서버에 수강신청 패킷을 보냅니다.
- **필수 데이터**: 과목번호(`suupNo`), 학수번호, 그리고 **NetFunnel Key**가 포함되어야 합니다.
- **검증**: 서버는 NetFunnel Key가 유효한지 확인하고, 유효하다면 수강신청을 처리합니다.

---

## 4. 이런 사이트 자동화(크롤링) 접근 방법

이런 사이트를 분석하고 자동화 프로그램을 만들 때의 일반적인 접근법입니다.

### 1단계: 관찰 (Observation)

- **도구**: 크롬 개발자 도구 (F12) > Network 탭
- **방법**: 'Preserve log'를 켜고, 실제로 로그인을 하고 수강신청 버튼을 눌러보면서 오고 가는 **모든 HTTP 요청(Request)과 응답(Response)**을 녹화(Capture)합니다.

### 2단계: 핵심 요청 식별 (Identification)

- 수많은 요청 중 "진짜" 데이터를 주는 놈과 "진짜" 동작을 하는 놈을 찾습니다.
  - 로그인 API는? (`lgnps.do`)
  - 수강신청 API는? (`hyuHaksaengSgsc.do`)

### 3단계: 동적 데이터 추적 (Dynamic Data Tracing)

- 단순히 URL만 알면 안 됩니다. 요청 데이터 중 **매번 바뀌는 값**이 무엇인지 찾아야 합니다.
  - `userId`가 이상한 문자열로 보인다? -> 암호화되었구나 -> 암호화 로직(JS 파일)을 분석해야 함.
  - URL 뒤에 `tk=...`가 계속 붙는다? -> 어딘가에서 이 값을 줬을 것이다 -> 이전 요청의 응답(HTML)을 뒤져본다.

### 4단계: 구현 (Implementation)

- **세션 유지**: `requests.Session()` 객체를 사용하여 쿠키를 자동으로 관리합니다.
- **헤더 모방**: `User-Agent`, `Referer` 등을 브라우저와 똑같이 설정하여 로봇이 아닌 척합니다.
- **로직 구현**: 분석한 대로 [로그인 -> 토큰추출 -> 대기열 -> 신청] 순서대로 코드를 짭니다.

### 요약

결국 **"브라우저가 하는 짓을 파이썬 코드가 똑같이 흉내내게 만드는 것"**이 핵심입니다. 서버 입장에서는 요청을 보낸 쪽이 크롬 브라우저인지 파이썬 스크립트인지 구별할 수 없도록(또는 구별하기 힘들도록) 데이터를 완벽하게 갖춰서 보내면 됩니다.
